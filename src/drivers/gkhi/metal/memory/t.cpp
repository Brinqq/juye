[frame graph]
//allocators

[frontend context]
//create_heap();
//create_texture(heap heap)
//create_buffer(heap heap)
//
//set_heaps() //before draw
//

[backend]

[metal]
// just doesnt handle complex allocation
// ratgher returns heap and allocates on it
// residecny sets is handled with set heaps before draw

