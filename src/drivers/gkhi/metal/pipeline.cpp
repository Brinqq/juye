#include "metal.h"


using namespace juye;

// state: 
//  - topolgy, winding, etc...
//  - Shaders: vertex, pixel, mesh, etc
//  - flags/features

// Attachments structure:
// vertex structure:
// parameter structure
gdi_pipeline* gdi_device::create_pipeline(gdi_pipeline_state_desc& state){
  MTL4::RenderPipelineDescriptor* desc;
};


// gdi_renderpass gdi_device::create_renderpass(){}
